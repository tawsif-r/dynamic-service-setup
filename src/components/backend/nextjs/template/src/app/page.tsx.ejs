export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '3rem' }}>
      <h1><%= config.name %></h1>
      <p>Generated with create-app. Health check at <code>/api/health</code>.</p>
    </main>
  );
}
